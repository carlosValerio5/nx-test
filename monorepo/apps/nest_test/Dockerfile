# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @monorepo/nest_test`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t monorepo-nest_test`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system monorepo-nest_test && \
          adduser --system -G monorepo-nest_test monorepo-nest_test

COPY package.json package-lock.json ./
RUN npm i --omit=dev -f install
COPY apps/nest_test/dist/ ./
RUN chown -R monorepo-nest_test:monorepo-nest_test .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

CMD [ "node", "main.js" ]
